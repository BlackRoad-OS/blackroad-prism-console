name: Deploy Quantum App
on:
  push:
    branches: [main]
    paths:
      - 'apps/quantum/**'
      - '.github/workflows/deploy-quantum.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
      - name: Copy to server
        uses: burnett01/rsync-deployments@d31bb62696a95cba655d64cb7a4d3c3eae27e3f4 # 6.0
        with:
          switches: -avzr --delete
          path: apps/quantum/
          remote_path: /var/www/blackroad/apps/quantum/
          remote_host: ${{ secrets.SSH_HOST }}
          remote_user: ${{ secrets.SSH_USER }}
          remote_key: ${{ secrets.SSH_KEY }}
      - name: Health check
        run: |
          curl -fsS "https://${{ secrets.SITE_DOMAIN }}/apps/quantum/ternary_consciousness_v3.html" >/dev/null

