name: Java 17 • Gradle

on:
  push:
    branches:
      - main
      - master
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Java 17 • Gradle
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4

      - name: Setup Java
        uses: actions/setup-java@7a6d8a8234af8eb26422e24052264383dc2730d1 # v4.6.0
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@ac638b010cf58a27ee6c972d7336334ccaf61c96 # v3.5.0
        with:
          cache-read-only: false

      - name: Run Gradle tests
        env:
          GRADLE_OPTS: "-Dorg.gradle.daemon=false"
        run: |
          if [ ! -f build.gradle ] && [ ! -f build.gradle.kts ]; then
            echo "No Gradle build file found. Skipping."
            exit 0
          fi
          if [ -x ./gradlew ]; then
            ./gradlew test --console=plain --stacktrace
          else
            gradle test --console=plain --stacktrace
          fi

      - name: Upload JUnit XML on failure
        if: failure()
>>>>>>>+main
===
            uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
>>>>>>>+origin/codex/pi
    with:
>>>>>>>+main
===
            uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
>>>>>>>+origin/codex/pi
      name: java17-gradle-reports
          path: |
            **/build/test-results/test/*.xml
            **/build/reports/tests/test
          if-no-files-found: ignore
name: Java 17 • Gradle

on:
  push:
    branches:
      - main
      - master
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Java 17 • Gradle
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4

      - name: Setup Java
        uses: actions/setup-java@7a6d8a8234af8eb26422e24052264383dc2730d1 # v4.6.0
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@ac638b010cf58a27ee6c972d7336334ccaf61c96 # v3.5.0
        with:
          cache-read-only: false

      - name: Run Gradle tests
        env:
          GRADLE_OPTS: "-Dorg.gradle.daemon=false"
        run: |
          if [ ! -f build.gradle ] && [ ! -f build.gradle.kts ]; then
            echo "No Gradle build file found. Skipping."
            exit 0
          fi
          if [ -x ./gradlew ]; then
            ./gradlew test --console=plain --stacktrace
          else
            gradle test --console=plain --stacktrace
          fi

      - name: Upload JUnit XML on failure
        if: failure()
<<<<<<<<        uses: actions/upload-artifact@26f96dfa697d77e81fd5907df203aa23a56210a8 # v4
>>>>>>>+main
===
            uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
>>>>>>>+origin/codex/pi
    with:
>>>>>>>+main
===
            uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
>>>>>>>+origin/codex/pi
      name: java17-gradle-reports
          path: |
            **/build/test-results/test/*.xml
            **/build/reports/tests/test
          if-no-files-found: ignore
