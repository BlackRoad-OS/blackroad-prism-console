name: Cleanup Preview Containers

on:
  pull_request:
    types: [closed]

permissions:
  contents: read
  packages: write

jobs:
  cleanup:
    name: Remove Preview Image Tag
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Delete PR preview tag
        uses: actions/delete-package-versions@e5254f5d4efb21e87869e82c2684b5a6f41e46b7 # v5
        with:
          owner: ${{ github.repository_owner }}
          package-name: ${{ github.event.repository.name }}
          package-type: container
          token: ${{ secrets.GITHUB_TOKEN }}
          tag-pattern: pr-${{ github.event.number }}-*
          min-versions-to-keep: 0
          delete-only-untagged-versions: false
