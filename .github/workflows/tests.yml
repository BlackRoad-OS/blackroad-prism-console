name: tests

on:
  push:
    branches: ["main", "master"]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pytest:
on: [push, pull_request]
jobs:
  run:
  node-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SAFE_MODE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4

      - name: Panic guard
        run: |
          if [ -f ".panic" ]; then echo "::error::.panic present"; exit 2; fi

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install -U pip wheel
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
          pip install pytest numpy scipy

      - name: Run tests (pytest)
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          pytest -q --junitxml=pytest.xml

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@26f96dfa697d77e81fd5907df203aa23a56210a8 # v4
        with:
          name: pytest-report
          path: pytest.xml

      - name: Run demo harness (optional)
        if: always()
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          if [ -f scripts/demo_amundson_math_core.py ]; then python scripts/demo_amundson_math_core.py | tee demo_output.txt; fi

      - name: Upload demo output
        if: always()
        uses: actions/upload-artifact@26f96dfa697d77e81fd5907df203aa23a56210a8 # v4
        with:
          name: demo-output
          path: demo_output.txt
      - uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '18'
          node-version: 20
      - run: npm install
      - run: npm test

  python-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest ruff
      - name: Check syntax
        run: python -m py_compile agents/auto_novel_agent.py agents/test_auto_novel_agent.py
      - name: Lint Python sources
        run: ruff check agents/auto_novel_agent.py agents/test_auto_novel_agent.py
      - name: Run Python tests
        run: pytest agents/test_auto_novel_agent.py
